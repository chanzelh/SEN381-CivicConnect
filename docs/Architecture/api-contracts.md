# API Contracts
## Conventions (auth method, response format, error-handling pattern)
## Endpoints — one subsection per core flow:
   - Submit request
   - Update request status
   - List/filter requests
   - Reporting/dashboard views
   For each: Method, Path, Request body, Response body, Status codes